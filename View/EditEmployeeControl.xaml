<UserControl x:Class="FanShop.View.EditEmployeeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FanShop.View"
             xmlns:windows="clr-namespace:FanShop.Windows"
             xmlns:utils="clr-namespace:FanShop.Utils"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#F2F9FF" Padding="15">
            <TextBlock Text="{Binding SelectedEmployee, Converter={x:Static local:EmployeeTitleConverter.Instance}}"
                       FontSize="16" FontWeight="SemiBold"
                       Foreground="#333333" HorizontalAlignment="Center"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="20,10">
            <StackPanel>
                <TextBlock Text="Фамилия:" FontWeight="Medium" Foreground="#333333" Margin="0,0,0,5"/>
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1"
                        CornerRadius="4" Padding="10,8" Margin="0,0,0,15">
                    <TextBox Text="{Binding Surname, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             utils:KeyboardNavigation.EnableArrowNavigation="True"/>
                </Border>

                <TextBlock Text="Имя:" FontWeight="Medium" Foreground="#333333" Margin="0,0,0,5"/>
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1"
                        CornerRadius="4" Padding="10,8" Margin="0,0,0,15">
                    <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             utils:KeyboardNavigation.EnableArrowNavigation="True"/>
                </Border>

                <TextBlock Text="Отчество:" FontWeight="Medium" Foreground="#333333" Margin="0,0,0,5"/>
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1"
                        CornerRadius="4" Padding="10,8" Margin="0,0,0,15">
                    <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             utils:KeyboardNavigation.EnableArrowNavigation="True"/>
                </Border>

                <TextBlock Text="Дата рождения:" FontWeight="Medium" Foreground="#333333" Margin="0,0,0,5"/>
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1"
                        CornerRadius="4" Padding="6,4" Margin="0,0,0,15">
                    <DatePicker SelectedDate="{Binding DateOfBirthPicker, UpdateSourceTrigger=PropertyChanged}"
                                BorderThickness="0" Background="Transparent" FontSize="14"
                                utils:KeyboardNavigation.EnableArrowFocusNavigation="True">
                    </DatePicker>
                </Border>

                <TextBlock Text="Место рождения:" FontWeight="Medium" Foreground="#333333" Margin="0,0,0,5"/>
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1"
                        CornerRadius="4" Padding="10,8" Margin="0,0,0,15">
                    <TextBox Text="{Binding PlaceOfBirth, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             utils:KeyboardNavigation.EnableArrowNavigation="True"/>
                </Border>

                <TextBlock Text="Паспорт:" FontWeight="Medium" Foreground="#333333" Margin="0,0,0,5"/>
                <Border Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="1"
                        CornerRadius="4" Padding="10,8" Margin="0,0,0,15">
                    <TextBox Text="{Binding Passport, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             utils:KeyboardNavigation.EnableArrowNavigation="True"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" Background="#F5F5F5" CornerRadius="0,0,8,8" Padding="0, 18, 0, 18">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Сохранить" Width="120" Height="36"
                        Background="#009EE1" Foreground="White"
                        BorderThickness="0" Margin="0,0,15,0" Cursor="Hand"
                        Command="{Binding SaveEditedEmployeeCommand}" 
                        IsEnabled="{Binding CanSaveEmployee}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4" Padding="12,6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Отмена" Width="120" Height="36"
                        Background="#666666" Foreground="White"
                        BorderThickness="0" Margin="0" Cursor="Hand"
                        Command="{Binding CancelEditCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4" Padding="12,6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
