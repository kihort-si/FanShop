<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:FanShop"
             xmlns:converters="using:FanShop.Converters"
             x:Class="FanShop.App"
             RequestedThemeVariant="Light">

    <Application.Styles>
        <FluentTheme /\u003e

        <StyleInclude Source="/Styles/ScrollBarStyles.axaml" />
        <StyleInclude Source="/Styles/DatePickerStyles.axaml" />
        <StyleInclude Source="/Styles/NonSelectableButton.axaml" />
    </Application.Styles>

    <Application.Resources>
        <ResourceDictionary>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter" />
            <converters:BoolToBackgroundConverter x:Key="BoolToBackgroundConverter" />
            <converters:DateToTodayColorConverter x:Key="DateToTodayColorConverter" />
            <converters:TimeSpanFormatConverter x:Key="TimeSpanFormatConverter" />
            <converters:ShiftToRussian x:Key="ShiftToRussian" />
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />

            <-- Modern ComboBox ToggleButton Style --\u003e
            <ControlTheme x:Key="ModernComboBoxToggleButton" TargetType="ToggleButton">
                <Setter Property="Template">
                    <ControlTemplate>
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid ColumnDefinitions="*, 30">
                                <ContentPresenter Grid.Column="0"
                                                  Margin="8,0,0,0"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}" />
                                <Path x:Name="Arrow"
                                     Grid.Column="1"
                                     Fill="#666666"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Data="M0,0 L8,0 L4,4 z" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter>

                <Style Selector="^:pointerover /template/ Border#Border">
                    <Setter Property="Background" Value="#F0F7FF" />
                </Style>

                <Style Selector="^:checked /template/ Path#Arrow">
                    <Setter Property="Fill" Value="#009EE1" />
                </Style>
            </ControlTheme>

            <-- Modern ComboBox Item Style --\u003e
            <ControlTheme x:Key="ModernComboBoxItem" TargetType="ComboBoxItem"
                          BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />

                <Style Selector="^:pointerover">
                    <Setter Property="Background" Value="#F0F7FF" />
                </Style>

                <Style Selector="^:selected">
                    <Setter Property="Background" Value="#E3F2FD" />
                    <Setter Property="Foreground" Value="#0077B5" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Style>
            </ControlTheme>

            <-- Modern ComboBox Style --\u003e
            <ControlTheme x:Key="ModernComboBox" TargetType="ComboBox"
                          BasedOn="{StaticResource {x:Type ComboBox}}">
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#CCCCCC" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="ItemContainerTheme" Value="{StaticResource ModernComboBoxItem}" />
            </ControlTheme>
        </ResourceDictionary>
    </Application.Resources>
</Application>
